# Node / Next.js
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# AI tool scaffolding - installed/vendored per developer, not project source.
# .kiro/settings/mcp.json in particular hardcodes an absolute machine path.
# .claude/launch.json is the one exception: it's the shared dev-server config
# run.bat depends on, so it's intentionally still tracked.
.claude/skills/
.claude/agents/
.claude/settings.local.json
.kiro/
.codex/
